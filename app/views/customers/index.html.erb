<p id="notice"><%= notice %></p>

<h1>Customers</h1>

<table class="table">
  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= link_to customer.name, customer %></td>
      </tr>
    <% end %>
    <tr>
      <td><%= link_to 'New Customer', new_customer_path %></td>
    </tr>
  </tbody>
</table>

<br>
